#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"

# Run every sync surface from one entry point so hooks only need one command.
"$ROOT_DIR/scripts/sync-cursor-skills"
"$ROOT_DIR/scripts/sync-codex-skills"
"$ROOT_DIR/scripts/sync-claude-skills"
"$ROOT_DIR/scripts/sync-agent-guidance"
